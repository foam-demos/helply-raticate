# Agent API

Core AI agent service for Helply. Processes support tickets, performs intent classification using LLMs, generates contextual responses, calculates confidence scores, and decides whether to auto-resolve or escalate. Built with Node.js, TypeScript, and OpenAI API.

## Local Development
```bash
npm install
npm run dev
```

Requires Postgres and Redis running locally.