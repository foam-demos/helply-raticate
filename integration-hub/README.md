# Integration Hub

Manages OAuth flows, webhook subscriptions, API sync, and rate limiting for all third-party integrations (Stripe, Zendesk, Freshdesk, Help Scout, Front, Crisp, Gong, PostHog, HubSpot, Attio). Built with Node.js, TypeScript, and Postgres for credential storage.

## Local Development
```bash
npm install
npm run dev
```